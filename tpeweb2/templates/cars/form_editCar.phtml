<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>editar vehiculo</title>
    <link rel="stylesheet" href="css/style.css">



</head>

<body>

    <h1>Editar vehículo</h1>

    <form action="<?= BASE_URL ?>editCar/<?= $car->id_vehiculo ?>" method="POST">


        <label for="id_marca">Marca:</label>
        <select name="id_marca" id="id_marca" required>
            <?php foreach ($marcas as $marca): ?>
                <option value="<?= $marca->id_marca ?>" <?= $marca->id_marca == $car->id_marca ? 'selected' : '' ?>>
                    <?= htmlspecialchars($marca->marca) ?>
                </option>
            <?php endforeach; ?>
        </select>
        <br>

        <label for="modelo">Modelo:</label>
        <input type="text" id="modelo" name="modelo" value="<?= htmlspecialchars($car->modelo) ?>" required>
        <br>

        <label for="año">Año:</label>
        <input type="number" id="año" name="año" min="1900" max="2099" value="<?= htmlspecialchars($car->año) ?>"
            required>
        <br>

        <label for="kilometraje">Kilometraje:</label>
        <input type="number" id="kilometraje" name="kilometraje" min="0"
            value="<?= htmlspecialchars($car->kilometraje) ?>" required>
        <br>

        <label for="version">Versión:</label>
        <input type="text" id="version" name="version" value="<?= htmlspecialchars($car->version) ?>" required>
        <br>

        <label for="motorizacion">Motorización:</label>
        <input type="text" id="motorizacion" name="motorizacion" value="<?= htmlspecialchars($car->motorizacion) ?>"
            required>
        <br>

        <label for="categoria">Categoría:</label>
        <input type="text" id="categoria" name="categoria" value="<?= htmlspecialchars($car->categoria) ?>" required>
        <br>

        <button type="submit">Actualizar vehículo</button>
    </form>


</body>

</html>